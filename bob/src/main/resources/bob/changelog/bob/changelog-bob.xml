<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
		http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <changeSet id="create_schema_bob" author="bob">
        <sql>
            create schema if not exists bob;
        </sql>
    </changeSet>

    <changeSet id="default_privileges_for_schema_bob" author="bob">
        <sql>
            GRANT USAGE ON SCHEMA bob TO termserver_app;
            ALTER DEFAULT PRIVILEGES IN SCHEMA bob GRANT USAGE ON SEQUENCES TO termserver_app;
            ALTER DEFAULT PRIVILEGES IN SCHEMA bob GRANT EXECUTE ON FUNCTIONS TO termserver_app;
            ALTER DEFAULT PRIVILEGES IN SCHEMA bob GRANT SELECT,INSERT,UPDATE ON TABLES TO termserver_app;

            GRANT USAGE ON SCHEMA bob TO termserver_viewer;
            ALTER DEFAULT PRIVILEGES IN SCHEMA bob GRANT EXECUTE ON FUNCTIONS TO termserver_viewer;
            ALTER DEFAULT PRIVILEGES IN SCHEMA bob GRANT SELECT ON TABLES TO termserver_viewer;
        </sql>
    </changeSet>
</databaseChangeLog>
